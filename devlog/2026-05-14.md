# 2026-05-14 开发日志

## 已完成

### 阶段 3：菜单栏 + UI（完成）
- NSPopover 替代手动 NSPanel 管理
- 玻璃风格 UI：`.allowsHitTesting(false)` 解决 overlay 阻挡点击的问题
- 弹簧弹出动画

### 阶段 4：核心交互（完成）
- 搜索：TextField 本地过滤
- 粘贴：点击卡片 → 写入剪贴板 → 通知关闭 Popover
- 置顶：pin/unpin DB 操作 + 列表刷新
- 删除：DB 删除 + 图片文件删除 + 列表刷新
- 一键清除：底部"清除"按钮，删除所有非置顶记录
- 点击动画：卡片缩放的弹簧动画反馈

### 阶段 5：设置 + 清理 + 开机自启（完成）
- AppSettings：UserDefaults 封装，保留时长 1/3/5 天
- SettingsView：分段选择器 + 开机自启注册按钮
- CleanupService：启动时 + 每小时自动清理过期数据
- SMAppService：注册开机启动

## 待办
（全部阶段完成）

## 备注
- `.overlay()` 会拦截点击事件，必须加 `.allowsHitTesting(false)`
- NSPopover 是菜单栏弹出面板的最佳方案，自动处理定位和外部点击关闭
- 自定义 `NSPanel` 子类并 override `canBecomeKey` 可以让面板接收键盘输入
